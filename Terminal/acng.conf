＃
＃ 重要的提示:
＃
＃这个文件可能只是这个目录中的一个配置文件。
＃在其他文件中设置的设置可以超出您在此处更改的值。走
＃查看其他配置文件！检查手册和安装说明
＃（如README.Debian）更多细节！
＃

＃这是apt-cacher-ng的配置文件，apt-cacher-ng是一个智能缓存代理
＃软件包下载。它应该在指定的目录中
＃apt-cacher-ng的-c选项，有关详细信息，请参阅apt-cacher-ng（8）。
#RULES:
＃变量名中的字母大小写无关紧要，名称和值应该是
＃用冒号分隔。对于布尔变量，零数被认为是假的，
#non-zero认为是真的。如果未明确提及默认值
#the description，注释值赋值主要代表默认值
＃特定变量的值。

＃下载数据和相关维护活动的存储目录。
＃
CacheDir:/var/cache/apt-cacher-ng

#log文件目录，可以设置为空以禁用日志记录
＃
LogDir:/var/log/apt-cacher-ng

＃如果不是，则查找其他配置和资源文件的位置
＃在配置目录中找到
＃
SupportDir:/usr/lib/apt-cacher-ng

＃传入http（或HTTP代理）连接的TCP服务器端口。
＃可以设置为9999来模拟apt-proxy。值0关闭TCP服务器
＃（在这种情况下必须设置SocketPath）。
＃
端口:3142

＃要侦听的地址或主机名。多个地址必须分隔
#space。每个条目必须是与a关联的精确本地地址
#local interface。使用getaddrinfo（3）对所有人执行DNS解析
#available protocol（IPv4，IPv6，...）。使用协议特定的格式将
#create binding（s）仅在协议特定的套接字上，例如0.0.0.0会
＃只收听IPv4。
＃
#Default:侦听所有接口和协议
＃
#BindAddress:localhost 192.168.7.254 publicNameOnMainInterface

＃另一个HTTP代理的规范，用于下载。
＃它可以包含用户名和密码，但请参阅手册以了解限制。
＃
＃默认:使用直接连接
＃
#Proxy:http://www-proxy.example.net:3128
#Proxy:https://username:proxypassword@proxy.example.net:3129

#Repository重映射。有关详情，请参阅手册
＃在此示例中，可能会在程序包期间生成一些后端文件
＃使用系统上收集的信息进行安装。
＃ 例子:
Remap-debrep:file:deb_mirror *.gz/debian; file:backends_debian＃Debian Archives
Remap-uburep:file:ubuntu_mirrors/ubuntu; file:backends_ubuntu #Ubuntu Archives
Remap-cygwin:file:cygwin_mirrors/cygwin＃; file:backends_cygwin＃incomplete，请在此处创建此文件或指定首选镜像
Remap-sfnet:file:sfnet_mirrors＃; file:backends_sfnet＃incomplete，请在此处创建此文件或指定首选镜像
Remap-alxrep:file:archlx_mirrors/archlinux＃; file:backend_archlx #Arch Linux
Remap-fedora:file:fedora_mirrors #Fedora Linux
Remap-epel:file:epel_mirrors #Fedora EPEL
Remap-slrep:file:sl_mirrors＃Scientific Linux
Remap-gentoo:file:gentoo_mirrors.gz/gentoo; file:backends_gentoo＃Gentoo Archives

#ansend.debian.org通常不需要这样做，因为它始终是
＃相同的DNS主机名。但是，可能会启用它以使用挂钩
＃或ForceManaged模式或此上下文中的特殊标志。默认情况下未设置。
#Repmap-secdeb:security.debian.org

＃可在Web浏览器中访问虚拟页面以查看统计信息和状态
#info，即在http://localhost:3142/acng-report.html下
＃注意:必须将此选项配置为运行维护作业（即使使用时）
#cron脚本中的acngtool）。 AdminAuth选项可用于限制
＃访问该页面上的敏感区域。
＃
＃默认:未设置，应由系统管理员设置
＃
ReportPage:acng-report.html

#Socket文件，用于通过本地UNIX套接字而不是TCP/IP进行访问。可
＃与inetd一起使用（通过apt-cacher-ng包中的桥接工具in.acng）。
＃
#Default:未设置，禁用UNIX套接字桥。
＃
#SocketPath:/var/run/apt-cacher-ng/socket

＃如果设置为1，则会在每个新行上将日志文件写入磁盘。默认
＃为0，客户端断开连接后刷新缓冲区。从技术上讲，
＃它是Debug选项的便利别名，详见下文。
＃
#UnbufferLogs:0

＃在日志表项中启用扩展客户端信息。仅设置为0时
记录＃活动类型，时间和传输大小。
＃
#VerboseLog:1

＃不要从启动控制台分离。
＃
#ForeGround:0

＃将守护进程的pid存储在指定的文本文件中。
＃默认:禁用
＃
＃PidFile:/var/run/apt-cacher-ng/pid

＃禁止传出连接并在没有互联网连接的情况下工作
＃响应503错误，这是不可能的。
＃
#Offlinemode:0

＃禁止从用户直接指定的位置下载
#request，即所有下载必须由预先配置的重新映射处理
#endends（见上文）。
＃
#ForceManaged:0

＃在考虑未引用文件过期（要删除）之前的天数。
#WARNING:如果值设置得太低而特定的索引文件不是
＃可用几天（镜像停机时间），然后有被删除的风险
＃仍然有用的包文件。
＃
ExThreshold:4

＃如果每天运行过期，有时候没有多大意义
＃it因为预期的更改（即删除过期的文件）不合理
＃额外处理时间或到期操作的额外下载
＃本身。如果本地客户端，这种差异可能会更加严重
＃安装很小或很少更新，但每天更改
＃远程归档元数据很重。
＃
＃以下选项启用了可能的权衡:到期运行是
＃抑制，直到通过下载一定数量的数据
自上次到期执行以来#apt-cacher-ng（可能表明这一点）
#packages被更新的版本所取代）。
＃
＃该号码可以有一个后缀（k，K，m，M表示Kb，KiB，Mb，MiB）
＃
#ExStartTradeOff:500米

＃出现严重问题时停止过期，问题就像更新失败一样
准备步骤中的索引文件的＃。
＃
＃警告:如果不考虑可能，请不要将此选项设置为零或为空
＃后果就像一个突然和完整的缓存数据丢失。
＃
#ExAbortOnProblems:1

＃被认为可接受的失败的夜间到期运行次数
＃不会向管理员触发错误通知（例如，通过每日cron作业）
＃达到（日）计数之前。可能对whacky互联网有用
＃连接。
＃
#Default:猜测值，如果ExThreshold为5或更大，则为1，否则为0。
＃
#ExSuppressAdminNotification:1

＃修改文件名以解决某些文件系统的限制。
＃警告:实验性功能，可能会有变化
＃
#StupidFs:0

#apt-listbugs的实验性功能:传递SOAP请求和
＃对bugs.debian.org的回复。
#Default:猜测值，true，除非启用了ForceManaged
＃
＃ForwardBtsSoap:1

#DNS解析数据有一个小的内存缓存，已过期
#this timeout（以秒为单位）。如果设置为值，则禁用内部缓存
＃小于零。
＃
#DnsCacheSeconds:1800

################################################## #############################
＃
＃警告:如果没有清除，请不要修改线程和文件匹配参数
＃想法背后发生的事情！
＃
#Max。连接线程的数量保持准备好（为了更快的响应）
＃未来。应该是介于0和平均连接数之间的合理值，
＃并取决于备用RAM的数量。
#MaxStandbyConThreads:8
＃
＃传入连接的活动线程计数的硬限制，即操作
达到此值时拒绝＃（低于零=无限制）。
#MaxConThreads:-1
＃
#Pegeonholing文件（如静态与易失性内容）由（扩展）完成
＃ 常用表达。
＃
＃以下模式可用于详细目的，其中
＃后者优先于前者:
＃ - «PFilePattern»表示在服务器上无静默更改的静态数据。
＃ - «VFilePattern»表示可能每小时都有变化的易变数据。档
与PFilePattern和VfilePattern匹配的＃将被视为volatile。
＃ - 文件名与VFilePattern匹配的静态数据可以被覆盖
＃通过使其与特殊静态数据模式匹配来视为volatile
＃«SPfilePattern»。
＃ - «SVfilePattern»或“特殊易失性数据”模式用于
＃方便指定与SPfilePattern匹配的任何异常，
＃对于仍必须将数据视为易失性的情况。
＃ - «WfilePattern»指定常规到期的“白名单模式”
#job，告诉它保留文件，即使它们没有被引用
＃others，例如加密签名，客户端开始下载。
＃
＃有两个版本。上面提到的模式变量不应该是
#set没有充分理由，因为它们会覆盖内置默认值
＃（可能会影响对apt-cacher-ng未来版本的更新）。有
＃也以Ex结尾的那些模式的版本，可以由。修改
#local administrator。除常规模式外，还会对它们进行评估
＃在运行时。
＃
＃要查看预期语法的示例，请运行:apt-cacher-ng -p debug = 1
＃
＃PfilePatternEx:
#VfilePatternEx:
#SPfilePatternEx:
＃SVfilePatternEx:
＃WfilePatternEx:
＃
################################################## #############################

＃位掩码类型值，声明错误的记录详细程度和行为
#log写作。非零值触发至少更快的日志文件刷新。
＃
＃一些高位只能使用apt-cacher-ng的特殊调试版本，
＃有关详细信息，请参阅手册。
＃
#Warning:这可以将大量数据写入apt-cacher.err日志文件。
＃
＃默认值:0
＃
#Debug:3

＃通常，像Squid这样的通用代理公开了IP的IP地址
＃使用X-Forwarded-For HTTP标头的远程服务器的客户端用户。这个
可以使用Expose-Origin选项打开＃behavior。
＃
#ExposeOrigin:0

＃记录始发IP地址时，请信任提供的信息
#X-Forwarded-For标头中的客户端。
＃
#LogSubmittedOrigin:0

＃向对等体报告的版本字符串，显示为HTTP客户端（和
镜像日志中的#version）。
＃
＃警告:期待副作用！一些档案使用此标题进行猜测
＃客户端的功能（即允许重定向和/或https链接）和
＃相应地改变他们的行为，但ACNG可能不支持预期
＃ 特征。
＃
＃默认:
＃
#UserAgent:又一个HTTP Client/1.2.3p4

＃在某些情况下，Import和Expiration任务可能会创建新的volatile
＃通过使用补丁文件重建它们以供内部使用的数据。这个
#by-product可能会被bzip2重新压缩，结果很幸运
#file与服务器上可以使用的*.bz2文件相同
#APT请求此文件的完整版本。
＃此功能的缺点是服务器上的CPU负载较高
＃维护任务，结果可能在LAN中没有太大价值
＃所有客户经常和定期更新他们的数据，因此通常
＃不需要索引文件的完整版本。
＃
#RecompBz2:0

＃传出连接的网络超时，以秒为单位。
＃
#NetworkTimeout:60

＃有时候将数据存储在缓存中并返回它是有意义的
＃将数据打包到客户端。以下DontCache *参数
＃可以为某些URL类型启用此行为。令牌被扩展
＃正在评估URL的正则表达式。
＃
#DontCacheRequested应用于从客户端进入的URL。
＃示例:排除使用kernel-package for x86构建的包
#DontCacheRequested:linux  - 。* _ 10\...\.Custo._i386
＃示例用例:从缓存中排除流行的私有IP范围
#DontCacheRequested:192.168.0 ^ 10\.. * 172.30
＃
#DontCacheResolved在映射到目标服务器后应用于URL。如果
＃指定多个后端服务器，然后只匹配
＃下载第一个可能来源的链接（由于实施限制）。
＃
＃示例用例:所有Ubuntu内容都来自本地镜像（指定为
#backend），不要再次缓存它:
#DontCacheResolved:ubuntumirror.local.net
＃
#DontCache指令设置（覆盖）DontCacheResolved和
#DontCacheRequested。为方便起见，请参阅这些指令
＃ 细节。
＃
＃示例:
#DontCache:。*。local.university.int

＃新创建的文件和目录的默认权限集，为八进制
＃numbers（有关详细信息，请参阅chmod（1））。
＃如果设置了，可以受umask值的限制（详见umask（2））
＃起始壳的环境，例如在apt-cacher-ng init脚本或
＃在其配置文件中。
＃
#DirPerms:00755
#FilePerms:00664

＃可以使用apt-cacher-ng作为有限的常规Web服务器
＃功能集，即目录浏览，任何文件的下载，Content-Type
＃基于/etc/mime.types，但没有排序，CGI执行，索引页面
#redirection和其他有趣的东西。
＃要获得此行为，请在虚拟目录和真实目录之间进行映射
必须使用LocalDirs指令定义服务器上的＃目录。
＃虚拟和真实目录由空格分隔，多对
＃由分号分隔。真实目录必须是绝对路径。
＃注意:由于该密钥目录的名称共享相同的名称空间
#repository名称（请参阅Remap -...）管理员的工作是避免冲突
＃之间的＃或明确创建它们。
＃
#LocalDirs:woo/data/debarchive/woody; hamm/data/debarchive/hamm
LocalDirs:acng-doc/usr/share/doc/apt-cacher-ng

#prepache由指定索引文件引用的一组文件。这可以使用
＃创建可用于离线工作的部分镜像。有一定的限制
＃和路径规范的限制，请参见手册和缓存控制
#web site了解详情。 （可能）相关索引文件的列表可以是
＃通过客户端计算机上的“apt-get --print-uris update”检索＃。
＃
＃示例:
#prepacheFor:debrep/dists/unstable/*/source/Sources * debrep/dists/unstable/*/binary-amd64/Packages *

＃要附加到通过线路发送的请求标头的任意数据集。应该
＃是一个格式良好的HTTP头部分，包括换行符（DOS样式）
＃可以作为转义序列输入（\ r\n）。
＃
#RequestAppendix:X-Tracking-Choice:do-not-track\r\n

＃指定用于远程连接的IP协议族。订单呢
＃matter，首先指定的是第一个。可能的组合:
＃v6 v4
＃v4 v6
#v6
#v4
#Default:使用系统的TCP/IP堆栈的本机顺序，受其影响
#BindAddress值。
＃
＃ConnectProto:v6 v4

＃常规到期算法查找不再列出的包文件
＃在任何索引文件中，并在安全期后删除它们。
＃此选项允许在缓存之后保留更多版本的包
＃安全期结束了。
＃
＃KeepExtraVersions:0

＃可选择使用libwrap提供的TCP访问控制，参见hosts_access（5）
＃了解详情。守护进程名称是apt-cacher-ng。
＃
＃默认:通过寻找apt-cacher-ng的明确提及在启动时猜到
#in/etc/hosts.allow或/etc/hosts.deny文件。
＃
#UseWrap:0

＃如果来自同一本地网络的许多计算机尝试更新索引文件
＃（apt-get update）几乎在同一时间，这些索引文件的已知状态
＃暂时冻结，多个请求收到缓存的响应
＃再次联系远程服务器。此参数（以秒为单位）
＃指定这些（易失性）文件之前的这段时间的长度
＃考虑过时了。
＃设置此值太低会传输更多数据并增加远程服务器
#load，设置太高（超过几分钟）会增加
＃向客户提供不一致响应的风险。
＃
＃FreshIndexMaxAge:27

＃通常不允许用户指定远程的自定义TCP端口
请求中的＃镜像，只能使用默认的HTTP端口（如
#opxy，代理管理员可以使用自定义端口创建Remap规则。
＃可以通过指定允许端口列表或0来禁用此限制
＃对于任何端口。
＃
＃AllowUserPorts:80

＃通常，HTTP重定向响应被转发给原始调用者
＃（即APT）从新URL开始新的下载尝试。这个
＃solution对于具有代理模式的客户端配置是可以的，但不起作用
＃well使用sources.list中的URL前缀进行配置。要解决
＃这个服务器可以使用重定向URL重新启动自己的下载，
＃使用以下选项配置。缺点是可能会使用它
＃以规避恶意用户的下载源策略。
#RedirMax选项指定允许服务器重定向的次数
＃按照每个请求，0禁用内部重定向。
＃默认:在启动时猜到，如果使用ForceManaged则为0，否则为5。
＃
#RedirMax:5

＃野外有一些破坏的HTTP服务器和代理服务器没有
＃正确支持If-Range标头，并在返回时返回错误数据
＃（volatile）文件的内容发生了变化。将VfileUseRangeOps设置为零
＃禁用基于范围的请求，同时检索易失性文件
#If-Modified-Since并请求完整文件。将其设置为
#a负值甚至删除If-Modified-Since标头。
＃
#VfileUseRangeOps:1

＃在客户端请求时允许某些主机的数据传递模式
＃使用CONNECT请求。这对于允许访问SSL特别有用
＃sites（https proxying）。字符串是一个应该覆盖的正则表达式
＃带端口的服务器名称，必须正确格式化和终止。
＃ 例子:
＃PassThroughPattern:private-ppa\.launchpad\.net:443 $
＃PassThroughPattern:。*＃这将允许CONNECT到一切
＃
＃默认:
＃PassThroughPattern:^ bugs\.debian\.org:443 $
PassThroughPattern:。*
＃邪恶的客户端可能会请求一个易失性文件，但却没有
＃检索响​​应并保持连接有效停留
＃许多小时，阻止特定文件进行其他下载尝试（其中
＃导致不向其他用户报告服务器端的文件更改。工作
＃around是在apt-cacher-ng中使用替代文件描述符，
＃但是由于缓存使用率较低，这可能会导致一些额外的下载流量。
#ResponfFreeDetectTime值指定文件描述符的时间
＃提到的状态被认为是缺陷，需要特殊处理。
＃默认时间为500秒。
＃
＃ResponseFreezeDetectTime:500

＃保持传出连接处于活动状态并重复使用以便以后下载
＃尽可能长的相同服务器。
＃
#ReuseConnections:1

＃批量发送到远程服务器之前的最大请求数
#response是预期的。使用更高的值可以大大提高平均值
＃吞吐量取决于网络延迟和远程执行
#servers。在客户端也启用时最有意义，请参阅apt.conf
＃documentation有关详细信息。
＃
＃默认值:如果设置了ReuseConnections，则为10，否则为1
＃
＃PipelineDepth:10

＃包含用于的可信CA证书的系统目录的路径
#outgoing connections，有关详细信息，请参阅OpenSSL文档。
＃
CApath:/etc/ssl/certs
＃
＃用于传出的单个受信任可信CA证书的路径
＃connections，有关详细信息，请参阅OpenSSL文档。
＃
CAfile:/etc/ssl/certs/server-ca.crt

＃有不同的方法可以检测到上游代理被破坏和转向
＃关闭它的使用并直接连接。第一个是通过自定义命令 - 
＃成功返回时，使用代理，否则不使用
＃命令将在指定的时间段后重新运行。
＃另一种方法是首先尝试连接代理并检测连接
＃ 超时。然后，连接将在没有HTTP代理的情况下终生
＃特定下载流的时间，也可能影响其他其他流
＃并行下载。
＃注意:此操作模式仍处于试验阶段，可能会有变化！
＃多个用户同时连接时可能会出现不需要的副作用
＃或具有特定的每个存储库代理设置。
＃
#Shell命令，默认:未设置。使用默认shell执行
#apt-cacher-ng进程用户的权限。例子:
＃/bin/ip route | grep -q 192.168.117
＃/usr/sbin/arp | grep -q 00:22:1f:51:8e:c1
＃
#OptProxyCheckCommand:......
＃
＃在几秒钟内检查intervall。
＃
#OptProxyCheckInterval:99
＃
#Conection timeout（秒），默认值:negative，表示禁用。
＃
#OptProxyTimeout:-1

＃可以限制下载代理的处理速度来设置
＃整体下载速度限制。单位:KiB/s，默认值:无限制。
＃
#MaxDlSpeed:500

＃在特殊的角落情况下，下载客户端会尝试下载随机块
文件头的数量，即第一个千字节。 “不要让客户陷入困境”
＃policy通常会将此转换为200响应，从主体开始
#start，但会混淆一些客户端。当此选项设置为a时
#tit value，这会修改行为并允许启动文件
＃下载可用数据与指定范围之间的距离
＃就在那个范围内。这看起来像是用户的随机延迟，但是
＃除此之外应该是无害的。
＃
#MaxInresponsiveDlSize:64000

＃在具有adhoc连接的移动环境中，重定向到某些连接
#id验证方，此重定向可能会因数据而损坏缓存
＃不自觉地存储为包数据。有一种尝试的机制
＃检测这种情况并通过不存储来减轻上述影响
#data并删除DNS缓存。触发器是a的出现
在最终下载目标的内容类型字段中的＃specific SUBSTRING
＃（即auth网站）和至少一个重定向。
＃
＃BadRedirDetectMime:text/html

＃收到BUS信号时（通常是IO错误），可以执行shell命令
＃在守护程序终止之前执行。
＃示例:
#BusAction:ls -l/proc/$ PPID/|邮件-s SIGBUS！根

＃仅设置此值以进行调试。它禁用SSL安全检查
＃像严格的主机验证。 0表示否，任何其他值都可以
＃differentrent意思是将来。
＃
#NoSSLChecks:0

＃设置此值意味着:从/通过缓存下载文件，标记相关
＃个文件。当acngtool运行shrink命令时，它会查看当天
＃上次从缓存中检索文件时（而不是上次时）
＃最初下载的）。
＃
＃TrackFileUse:0

＃控制支持此功能的文件系统空间的预分配。
＃这可能会减少磁盘碎片，从而改善以后的读取
＃表现。但是，可以降低写入性能
＃被恶意用户利用。
＃该值定义了按预期方式向操作系统报告的大小限制
#file size（从文件开头）。
＃设置为零以完全禁用此功能。默认值:一兆字节
＃
＃ReserveSpace:1048576